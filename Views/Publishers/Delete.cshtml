@model FPTBookS.Models.Publisher

@{
    Layout = "~/Views/_LayoutHome.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Delete</title>
</head>
<body>

    <div>
        <h4>Delete Publisher</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.PublishID)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.PublishID)
            </dd>
            <dt>
                @Html.DisplayNameFor(model => model.PublishName)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.PublishName)
            </dd>


        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value="Delete" class="btn btn-default" /> |
                @Html.ActionLink("Back to List", "Index")
            </div>
        }
    </div>
    <h3>Are you sure you want to delete this?</h3>
</body>
</html>
